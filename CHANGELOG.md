# Changelog

Keep a Changelog, SemVer. Every commit that touches source, tests, scripts, CI, migrations or docs adds a line under Unreleased in the same commit (CHANGE.1, CHANGE.2).

## [Unreleased]

### Changed

- Abatty 0.4.0 → 0.6.0: the hooks and harness brought to 0.6.0 (`abatty update`); the redefined `docs.behindCode` floor re-recorded (`abatty baseline`, every floor unchanged, three new metrics at 0); every browser spec now takes `test` from `e2e/fixtures.ts`, which fails a test whose page throws or does not hydrate (TEST-E2E-ERRORS). CI actions are pinned to commits, the findings and the conformance statement fail on a crash, and a new `floors` job wants a second approval for a loosened floor; the regenerated workflow is not used as is (its browser job has no database, its bypass step is not valid YAML, its scrub step fails with scrub off), so these changes are applied to ours by hand. The browser suite now runs on its own database (`TEST_DATABASE_URL`, migrated and seeded by `pree2e`; Redis index 1 via `suiteEnv`), never on the dev database, or is deferred to CI. The harness files are no longer reformatted by Prettier, which `abatty doctor` read as drift.

### Fixed

- Browser tests failed now and then by typing into a page before React had hydrated it: hydration then rewrote the field with the server's text (a textarea got it appended, a list gained a duplicate). Every spec now navigates through `open()`, which waits for the page's controls to hydrate (`HydrationMark`, and each control's React fiber behind the office's loading boundary); verified against the production build.
- The quotation editor browser test hard-coded a destination total, which broke when an earlier run had left a document for the same random country code (one of 9); it now compares the total before and after, and draws the code from 676.
- Saving a document rule or a holiday re-planned every live booking one at a time (a few queries and inserts each), about 30 s at 400 bookings and growing with every booking the office keeps. The bookings, their steps and what their quotations sold are now read in three queries and the new steps and audit lines written in batches; the outcome is the same.
- Invoicing a booking from its quotation also offered lines taken off the quotation and lines of declined destinations; it now offers only the live lines of the booking's own destination.
- The Activity browser test failed once a task in the database went overdue: its "Due" field locator also matched the "Overdue" section. It now matches the label exactly.
- Postgres and Redis stayed down after Docker Desktop restarted, and login failed with ECONNREFUSED: both services now restart with Docker (`restart: unless-stopped`).
- The live-updates stream threw "Controller is already closed" (an uncaught exception on the server) when a browser left while a message was on its way: the stream now stops once, whatever ends it first, and writes nothing after.
- The Journal drew a whole year at once and took 6–15 s to open; it now shows 100 entries a page, opening on the latest (the account ledger 200 lines a page, searchable too, each line keeping its true running balance), and can be searched by number, partner or label. Other long lists are bounded the same way: Contacts 100 a page, and each Activity bucket draws its first 50 tasks with the count of the rest.
- A new contact could stay missing from the customer and supplier pickers for up to five minutes: a shorter-lived cached list sharing the `contacts` tag shortened the tag's life below the pickers' key, so creating a contact no longer cleared it. A tag set's expiry now only ever rises.
- A bank line's de-duplication key now includes the payer's name: two payers sending the same amount with the same note on the same day were taken for one.
- Forms lost everything typed after a validation error (React 19 resets `<form action>` once the action returns); all stateful forms now submit through `ActionForm`.
- Success toasts never appeared when the action removed its own component (archive, cancel, remove container); toasts now fire when the result arrives (`useToastedAction`).
- Emptying an optional field (contact phone, booking vessel…) did not clear it, because Drizzle skips `undefined`; updates now send null (`nullMissing`).
- CI ran the opt-in scrub scan that abatty.config.json disables, failing every push; the step is removed.
- The generated CI workflow had an invalid YAML line (bypass-rate step), now a block scalar.

### Added

- Light and dark mode: a toggle in the header (light → dark → system; the system's by default, remembered per browser, no flash on load) on a light palette of the same family as the legacy dark one, every text token at 4.5:1 or better on its surfaces.
- Accessibility (WCAG 2.2 AA): a skip link to the content and a main landmark; focus moves to the page heading on a client-side navigation; a visible focus ring on every link; motion honours the reduced-motion setting; the reason dialog's control is named; rows and cards switched off or archived are muted by colour, not by opacity, which had pushed their badges under 4.5:1; the dark red lightened for the same reason; buttons that are links are rendered as links. The browser suite runs axe (WCAG 2.2 AA + best practice, nothing serious or critical) over the login page, seventeen office screens in both themes, a contact, a quotation, every tab of a booking and an open dialog.
- Discuss — real sending: with SMTP_URL (and MAIL_FROM) set, an outgoing e-mail written from a booking, a quotation's letter and a payment reminder leave the server themselves and are marked delivered; with the WhatsApp Business Cloud API configured (WHATSAPP_PHONE_ID, WHATSAPP_TOKEN), a WhatsApp does the same. Unset, nothing changes: the message is recorded and opened in the person's own app, marked "not sent yet". A server's refusal is said and the letter handed to the app instead. New dependency: nodemailer (MIT), server only.
- Settings — four tables the office edits: Ports (the legacy table of 303 UN/LOCODEs with name and country, one per line, offered on every port field as you type — a code not in the list is still accepted); Filing rules (the words in a file's name and the code it is filed under); Permissions (the roles' matrix, a checkbox per permission and role, applied to every gate and every screen on the next page load; the Admin keeps Settings); and the Links report — shipping lines named on sailings or catalogue legs with no contact of their name, bookings with no quotation behind their price, customers shipping without a VAT number.
- Home — the activity centre: the launcher shows each app the role opens with what it holds (open tasks overdue · today · upcoming, quotations and how many in follow-up, live bookings, contacts and their extra addresses, messages, issued invoices), and the month's bookings against the previous month, counted by sailing date (bookings, containers, destinations, in transit, arrived, value for roles that see prices, overdue document steps). The "migration in progress" card is gone: every app runs on the new stack.
- Activity, per-container rules: a document rule can make one step per container (Settings › Document rules, "One step per container"); the legacy CERTIWEIGHT rule is back this way — sold on the quotation, one step per box, its certificate filed against the box's step settles that box only, a box that leaves the booking takes its step with it, and once a certificate is filed the Certiweight line cannot be taken off the quotation. A rule can also wait until every box's cargo weight is in ("Only once every box's weight is in", the legacy weights check on the VGM). A default rule the office's saved rule book lacks is appended to it, so CERTIWEIGHT reaches every office; a changed default (the VGM waiting on the weights) is the office's row to tick.
- Bookings, step 2 — the printed copies (Summary tab): the customer copy with the shipment, the closings, the containers and the price of the booking's destination on the quotation, itemized or all-inclusive as the quotation chose, with VAT per rate and the exemption mention; the trucker copy with no price — one box per driver (or all on one sheet), its loading address, date and hour, seal, weights and VGM (over the limit named as such), and the cut-offs; a detail not agreed prints as missing, never another box's. The letterhead and footer of every printed document (invoice, quotation, copies) are one component.
- Bookings, step 1 — the Documents tab: files on a booking (PDF, photo, sheet, Word, CSV, text or a mail, up to 25 MB) stored under FILES_DIR (one folder per booking, gitignored; the database keeps what each is), opened from the office, filed under a code read from the file's name (Settings table fileHints, seeded from the legacy filing rules) or chosen, as a draft or a final; a final filed against a document step marks the step done and opens what waited on it; refiled, or taken off with a reason, never deleted. The tab lists the papers the shipment still lacks (the destination country's documents from the catalogue, and each step's paper) with their state — missing, draft, filed. Migration 0019.
- Quotations, step 3 — presentation, print and send: a quotation is itemized (every line with its price) or all-inclusive (one price per destination, naming the services chosen to be named); the printable quotation (letterhead, destinations, totals, VAT note; declined destinations left out) prints to PDF from the browser. Send fills the Quotation letter (new QUOTE_OUT template in Settings › Templates: destinations, prices, total, validity), to the customer's e-mail or WhatsApp; once read and changed, it is recorded on the quotation's messages, a draft becomes Sent with the day and channel, a follow-up task for tomorrow asks whether the customer agrees, and the mail app or WhatsApp opens with the text. Templates whose code starts with QUOTE_ belong to quotations and are no longer offered on a booking; a default template added later now reaches an office that saved its template table before. Migration 0018.
- Quotations, step 2b — the destination and line editor: a quotation takes more destinations, each started from an ocean leg of the catalogue (its ports and box, with the leg and the documents its country requires as first lines) or typed; a destination is edited, marked declined by the customer with a reason (refused once booked) and put back. Lines are added from the catalogue — priced for the customer: the agreement in force, then the last price quoted if the contact is set to it, then the catalogue — or typed with their own price; each shows where its price came from (Agreed, Last price, Catalogue, Typed), a sell price changed by hand becomes Typed, and costs stay hidden from roles without costs.view. A line is taken off with a reason, never deleted, and not once invoiced; its quantity cannot go below what was invoiced. Every change goes through the quotation's version (a colleague's edit in between is a conflict), and a booked destination's "only if sold" steps follow its lines. Migration 0017.
- Quotations, step 2a — a booking ships one destination: each destination of a quotation is accepted on its own and becomes its own booking (legacy b.routeId); a destination already booked, or declined, is refused. A booking's price, invoicing, costs to receive, the customer's credit exposure and its "only if sold" document rules now read that destination's lines only, not every destination of the quotation. Existing bookings are linked to their quotation's first destination. Migration 0016.
- Quotations, step 1 — the rate catalogue and customer agreements: Settings › Catalogue holds every chargeable item (ocean legs, inland moves, customs, country documents, VGM, free time, extras) with its buy and sell price, VAT code, contract or spot rate and validity, each category asking for what identifies it (ports, places, document and country, free days, a name); items are taken out of use with a reason and put back, never deleted. Settings › Price lists holds each customer's agreements: agreed sell and buy per item for a period (an empty price takes the catalogue's), one agreement in force per day — a second on the same days is refused — and a changed or removed price stays on the record. The contact page lists its agreements. The categories and their sales/purchase accounts are a Settings table (`rateCategories`) seeded from the legacy split. New permission catalogue.edit (Admin, Accountant, Team lead). Migration 0015.
- Document rules — "only if sold": a rule can name the service it depends on (a word, or a pattern such as vgm|certiweight); it then applies only to bookings whose quotation has a line for it — what we didn't sell is not our job (invariant 6). A booking with no quotation behind it keeps every rule. Edited in Settings › Document rules.
- Discuss — message templates: the letters the office writes again and again (booking confirmation, trucker copy, export invoice request, shipping instruction, departure notice…) as a Settings table; on a booking's Messages tab a template fills the subject and the text from the file (client, route, containers, vessel, dates, closings, loading) and stays editable; a placeholder the file cannot fill shows "—". Settings › Templates edits, adds and takes templates out of use.
- Discuss — routing and escalation: Settings › Routing edits the routing table (each topic's subject, the role it goes to, on or off; new topics added, OTHER always kept on) and the escalation delay. A message nobody takes within that many minutes (30 by default) also appears in the Team lead's queue, marked with how long it has waited.
- Activity — away & cover: an Admin or Team lead records that somebody is away (from a day, until a day or open-ended) and who covers; for those days the absent person's tasks — and new ones meant for them — appear in the colleague's "My tasks", nothing is moved, and "Back now" gives them back at once. A colleague already away, or a second cover for the same days, is refused. "Hand over for good" moves every open task of a person (or the untaken tasks of a role) to someone, once, on the record — each task a version on. New permission activity.cover. Migration 0014.
- The vessel register (Settings › Vessels): sailings — ship, voyage, carrier, service, route, ETD/ETA, sailed and arrived dates, state — searchable, soonest first. A booking is put on a sailing from its Edit tab and takes its ship, voyage, ETD, ETA and closings (customs, SI & docs, VGM, port cut-off, each so many days before the ETD, editable in Settings); when the sailing moves, every live booking on it moves with it and its document steps are re-planned. Saving new closing offsets re-dates every booking on a sailing. Migration 0013.
- Contacts: VAT and EORI numbers are kept clean ("be 0464.648.410" is BE0464648410) and checked against their country's format (legacy ID_FORMATS; Belgian enterprise numbers also by their check digits) — a wrong one is refused with what the country expects and an example; the same VAT number on two contacts is refused, naming the other. Child addresses and persons (loading, delivery, invoicing, consignee…, from the Settings list of kinds) are added and removed with a reason on the contact; call, WhatsApp and e-mail in one tap.
- Accounting, step 12 — the Odoo cut-over: Odoo's CSV exports come over in order — contacts (matched by VAT number or name, completed but never overwritten, their IBAN kept), the customer invoices and supplier bills still open on the cut-over day (each an opening document of its open part against 499000 under Odoo's own number, left out of the VAT return and the listings; our series continue after Odoo's highest number), and the trial balance as one opening entry (customers and suppliers to 499000, the year's result to 140000, refused unless it balances) — then the books close through the cut-over day. Importing twice changes nothing. Migration 0012.
- Accounting, step 11 — Peppol e-invoicing: each issued invoice and credit note gives its UBL 2.1 file under Peppol BIS Billing 3.0 (tax subtotals per category with the exemption reasons, parties addressed by their Peppol scheme, the structured reference and our IBAN; the credited invoice on a credit note), refused with the reason when the customer has no VAT number or address. A supplier's Peppol file imported on the Bills screen becomes a draft bill: the supplier found by VAT number or added as a contact, their IBAN kept for paying them, their number and dates carried into the recording form, the same number twice refused, and a total our VAT does not reproduce noted on the bill. New dependency: fast-xml-parser (MIT), used only on the server.
- Accounting, step 10 — costs to receive (accruals): for shipments sailed by a day, the cost the quotation expected less the supplier bills recorded; booked on a past day as 604000 per shipment against 444000 Invoices to receive and reversed the next day, kept as booked so later bills never shift it; one live run per day, cancelled with a reason while the day is open. Journal and ledger entries now link to the screen that made them (assets, accruals). Migration 0011.
- Accounting, step 9 — fixed assets: a supplier bill line on an equipment account (class 2) becomes an asset when the bill is recorded (IT over 3 years, other equipment over 5); depreciation is straight line, month by month from the month bought, booked in the journal at each finished month's end (630200 / 230900) with the cents left on the last month; the years can change until a booked month is closed; disposing of an asset (not in the future, not in a closed period) stops depreciation before that month and books what is left as a loss (663000) on the day. Assets screen with cost, depreciated and book value. Migration 0010.
- Accounting, step 8 — paying suppliers by SEPA: a contact's bank accounts (IBAN checked mod 97, BIC, label; removed with a reason, never deleted); Pay suppliers lists the open bills with the supplier's IBAN and why any cannot be paid (not approved, no or invalid IBAN, already in a file), and makes an ISO 20022 pain.001.001.03 file for the ticked ones (their number and ours as the remittance, a structured reference sent as one) — bills are locked and re-checked so one bill never goes in two files; the file is kept to download again, and a file the bank refused is cancelled with a reason to release its bills. Payments are booked when the statement shows them, as before. Migration 0009.
- Accounting, step 7 — reminders and the credit limit: a Reminders screen lists what is overdue and the reminder due on each (friendly after 1 day, second after 15, last notice after 30, never twice within 10 days); the reminder is written out with what is open and how to pay, recorded in the messages and on the invoice, and opened in the mail app — the step is re-checked under a lock so it cannot go twice. A customer's exposure (open invoices plus what their live bookings will still invoice) shows against their credit limit on the contact page, and issuing an invoice past the limit warns. Migration 0008.
- Accounting, step 6 — listings and the accountant's export: the annual listing of Belgian customers (over €250 net of credit notes) and the intra-community listing of reverse-charge services per VAT period, each with a draft Intervat XML; the journal of any period as a CSV for the accountant (";" separated, comma decimals, readable by Excel).
- Accounting, step 5 — VAT and closed periods: the Belgian VAT return (form 625) per quarter or month, from the documents dated in it — sales per rate in 00/01/03, exempt export in 47, EU reverse charge in 44 and 48, credit notes in 49/64, purchases in 82/83 with deductible VAT in 59, and the reverse charge on services bought (88/55 from the EU, 87/56 from elsewhere) — with its deadline, a draft Intervat XML, and "Mark as filed", which keeps the grids as sent and closes the books through the period's end. Closing the books (only through a past day, only forward) refuses anything dated in a closed period: issuing, crediting, recording a bill, booking or reversing a payment. Migration 0007.
- Accounting, step 4 — the books: a journal derived from every numbered document and payment (sales invoices and credit notes on 400000/70x/451000, supplier bills on 440000/60x-61x/411000 with both sides of the reverse charge, payments on 550000 with write-offs, reversals as their own entry), never typed in and refused if it does not balance; Journal, Reports (profit and loss, balance sheet, trial balance with drill-down to each account's ledger), Aged receivables and payables, and Margins per shipment, each over a chosen period.
- Accounting, step 3 — supplier bills: record a supplier's invoice (for the office or as a cost of a booking, lines on purchase accounts 604000/61x/230000) in our own BILL/YYYY/NNNNN series with their number; the same supplier number cannot be recorded twice; from €5,000 a second person approves before payment (four eyes, refused for the one who recorded it); bills are paid by hand or from outgoing bank lines matched by our number, their number or their IBAN + amount; a booking's Billing tab shows its costs and margin. Migration 0006.
- Accounting, step 2 — payments and the bank: register payments against an invoice (never more than is open; a shortfall can be written off to 657000/658000/758000), reverse them with a reason and a date (never deleted); the invoice's paid / partly paid / overdue state is read from the ledger each time. Import CODA or bank CSV statements (EN/FR/NL headers, both decimal conventions; overlapping exports skipped by a de-duplication key); each line proposes the invoice it pays — structured communication, invoice number in the text, known IBAN with the exact open amount, or the amount alone — and auto-match books only the certain ones. Payments and Bank screens. Migration 0005.
- Accounting, step 1 — sales invoices and credit notes: invoice a booking from its quotation lines (partially, to any party on the booking; over-billing refused again at issue), drafts with extra lines, issue with the unbroken yearly series INV/YYYY/NNNNN taken in the issuing transaction, due date from the payment term, +++OGM+++ reference, VAT per rate with the legal mention of each exempt code, credit notes in their own series with an optional corrected draft, discarding drafts with a reason, a printable invoice, and a Billing tab on each booking. Migration 0004.

### Fixed

- The Accounting screens no longer carry two h1 headings (layout title plus page title).
- Discuss: one message store for e-mail, WhatsApp, calls, the website and internal chat. Internal rooms per role and for the whole office (a bare SB/QT number links the message); a waiting queue routed by topic to a role, where the first to take a message owns it and anything answered on the same file leaves the queue; logging a message received elsewhere; writing from a booking with the subject key [SB…/MSG], recorded and opened in the mail or WhatsApp app, so replies re-thread by the key; the message guard flags a sender who is not a party on the booking; screens refresh live through Redis pub/sub and Server-Sent Events. Migration 0003.
- Document rules engine (legacy DOC_RULES): a pure, tested engine picks the rules for a booking (destination from the port of discharge's UN/LOCODE, loading-port rules beating general ones, export/import/return), dates each step from its anchor (working days skip weekends and BE + destination holidays; a calendar-day deadline on a closed day moves back and says why), and fails closed on unknown prerequisites. A sync opens steps as tasks once their prerequisites are done, redates open steps when the booking's dates move, and withdraws steps whose rule no longer applies. The legacy rule book and 2026 holidays ship as defaults; Settings › Document rules and Holidays edit them and re-plan every live booking. Bookings gain customs/VGM/SI closing and port cut-off dates and a Documents tab; blocking steps show ⛔.
- Activity: My tasks (assigned to me, or to my role and not yet taken), Everyone and per-person views, Overdue · Today · Upcoming · No date buckets, search by task or SB ref, create, done/reopen, withdraw/put back with a reason, hand over to a person or a role, a Monday-first month calendar, a Tasks tab on each booking, and my overdue/today counts on Home. State moves are a tested domain rule (`nextState`).
- Bookings: Edit tab (parties by contact id, route, loading, vessel/voyage, ETD/ETA guard, carrier and B/L numbers), Containers tab (ISO 6346 numbers, seals, tare/cargo, VGM against the type maximum, add/remove with a reason) and History tab.
- Playwright browser suite (smoke, booking journey, settings), run locally against the dev database and in CI against a fresh Postgres and Redis.
- The unguarded-server-function probe also scans cached const queries and `*-actions.ts` files.
- Settings: People (add, change role, switch off/on; nobody switches themselves off and one active Admin always remains), Lists editor over `config_tables` with optimistic versions, Audit log viewer, and My account (change password, other sessions signed out).
- GitHub Actions workflow running the same gate as the pre-push hook (`abatty ci`), plus database and build jobs.
- Next.js 16 app (App Router, Turbopack) replacing the legacy single-file TMS, strangler-style.
- Postgres schema via Drizzle (14 tables: identity, contacts, quotations, bookings, containers, activities, config tables, sequences, audit) and the initial migration.
- Redis read-through cache with tag invalidation and a Redis-backed login rate limiter, both degrading gracefully without Redis.
- Auth: bcrypt passwords (legacy hashes compatible), hashed DB sessions, DAL permission checks, Next 16 proxy redirect.
- Domain layer ported from the legacy engine with unit tests: clock-free dates, QT/SB refs, OGM, IBAN, ISO 6346, permissions matrix, VAT defaults.
- Contacts (list, search, create, edit, archive with in-use guard, put back), Quotations (create, view, accept → booking), Bookings (create with SB in transaction, status, cancel with reason, put back).
- shadcn/ui (base-nova, Base UI) with the legacy dark palette; app shell with permission-filtered sidebar.
- The engineering standard's instrument: harness, gate, import graph, dead code (`abatty init`), with project probes for the legacy invariants (`abatty.probes.mjs`), project rules (`abatty.rules.mjs`) and the layer boundary map in `.dependency-cruiser.cjs`.
